import React from "react";

const Content = (props) => {
    return (
        <>
            <p>{props.first_name}</p>
            <p>{props.last_name}</p>
        </>
    );
};
export default Content;
